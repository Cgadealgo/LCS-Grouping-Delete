from setuptools import setup, find_packages

setup(
    name='sortdelete',
    version='1.0',
    description='A module to process and group PPT files based on similarity',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
)
